{
  "name": "snooker-match",
  "version": "1.0.0",
  "description": "Single-camera on-device snooker match monitor: detection pipeline, deterministic rules engine, local-network hub UI with TV view, corrections, and calibration.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/main.ts",
    "cameras": "bun run scripts/list-cameras.ts",
    "train": "bun run src/trainer/train-cli.ts",
    "test": "bun test",
    "typecheck": "bunx --package typescript tsc --noEmit",
    "bundle": "bun run scripts/bundle.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
